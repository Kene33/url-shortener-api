server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ { proxy_pass http://app:8000; }
    location /health/ { proxy_pass http://app:8000; }
    location ~ ^/(login|register|verify-email|forgot-password|reset-password|links|analytics|folders|settings|profile|notifications)(/.*)?$ {
        try_files $uri $uri/ /index.html;
    }
    location ~ ^/admin(/.*)?$ {
        try_files $uri $uri/ /index.html;
    }
    location ~ ^/[A-Za-z0-9]{6,32}$ { proxy_pass http://app:8000; }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
